# StudyFlow 前端

本目录是 StudyFlow 的 React + TypeScript 前端工程。

## 常用命令

```powershell
npm install
npm run dev
npm run build
```

## 目录说明

- `src/api`：封装后端接口请求。
- `src/pages`：页面组件，例如登录、注册、仪表盘。
- `src/routes`：路由守卫，例如未登录时跳转登录页。
- `src/main.tsx`：前端入口，挂载 React、路由、React Query 和 Ant Design。
