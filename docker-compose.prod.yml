services:
  backend:
    image: ${IMAGE_REGISTRY:-ghcr.io/violetztr/study-flow}/backend:${IMAGE_TAG:-latest}

  frontend:
    image: ${IMAGE_REGISTRY:-ghcr.io/violetztr/study-flow}/frontend:${IMAGE_TAG:-latest}

  prometheus:
    image: prom/prometheus:v2.54.0

  grafana:
    image: grafana/grafana:11.1.0
